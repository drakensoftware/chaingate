diff --git a/node_modules/bitcore-lib-cash/index.js b/node_modules/bitcore-lib-cash/index.js
index a562c53..9b2b312 100644
--- a/node_modules/bitcore-lib-cash/index.js
+++ b/node_modules/bitcore-lib-cash/index.js
@@ -12,8 +12,9 @@ bitcore.versionGuard = function(version) {
     throw new Error(message);
   }
 };
-bitcore.versionGuard(global._bitcoreCash);
-global._bitcoreCash = bitcore.version;
+// Disabled: version guard pollutes globalThis and breaks bundlers.
+// bitcore.versionGuard(global._bitcoreCash);
+// global._bitcoreCash = bitcore.version;
 
 // crypto
 bitcore.crypto = {};
diff --git a/node_modules/bitcore-lib-cash/lib/crypto/hash.js b/node_modules/bitcore-lib-cash/lib/crypto/hash.js
index 189e8f3..934d16f 100644
--- a/node_modules/bitcore-lib-cash/lib/crypto/hash.js
+++ b/node_modules/bitcore-lib-cash/lib/crypto/hash.js
@@ -1,6 +1,12 @@
 'use strict';
 
-var crypto = require('crypto');
+// Replace Node.js crypto with @noble/hashes (CommonJS syntax)
+// Use .js-suffixed paths to match @noble/hashes v2.x package.json "exports" field.
+var nobleSha256 = require('@noble/hashes/sha2.js').sha256;
+var nobleSha512 = require('@noble/hashes/sha2.js').sha512;
+var nobleSha1 = require('@noble/hashes/legacy.js').sha1;
+var nobleRipemd160 = require('@noble/hashes/legacy.js').ripemd160;
+
 var BufferUtil = require('../util/buffer');
 var $ = require('../util/preconditions');
 
@@ -8,16 +14,14 @@ var Hash = module.exports;
 
 Hash.sha1 = function(buf) {
   $.checkArgument(BufferUtil.isBuffer(buf));
-  return crypto.createHash('sha1').update(buf).digest();
+  return Buffer.from(nobleSha1(buf));
 };
-
 Hash.sha1.blocksize = 512;
 
 Hash.sha256 = function(buf) {
   $.checkArgument(BufferUtil.isBuffer(buf));
-  return crypto.createHash('sha256').update(buf).digest();
+  return Buffer.from(nobleSha256(buf));
 };
-
 Hash.sha256.blocksize = 512;
 
 Hash.sha256sha256 = function(buf) {
@@ -27,7 +31,7 @@ Hash.sha256sha256 = function(buf) {
 
 Hash.ripemd160 = function(buf) {
   $.checkArgument(BufferUtil.isBuffer(buf));
-  return crypto.createHash('ripemd160').update(buf).digest();
+  return Buffer.from(nobleRipemd160(buf));
 };
 
 Hash.sha256ripemd160 = function(buf) {
@@ -37,9 +41,8 @@ Hash.sha256ripemd160 = function(buf) {
 
 Hash.sha512 = function(buf) {
   $.checkArgument(BufferUtil.isBuffer(buf));
-  return crypto.createHash('sha512').update(buf).digest();
+  return Buffer.from(nobleSha512(buf));
 };
-
 Hash.sha512.blocksize = 1024;
 
 Hash.hmac = function(hashf, data, key) {
@@ -53,7 +56,7 @@ Hash.hmac = function(hashf, data, key) {
 
   if (key.length > blocksize) {
     key = hashf(key);
-  } else if (key < blocksize) {
+  } else if (key.length < blocksize) {
     var fill = Buffer.alloc(blocksize);
     fill.fill(0);
     key.copy(fill);
diff --git a/node_modules/bitcore-lib-cash/lib/crypto/random.js b/node_modules/bitcore-lib-cash/lib/crypto/random.js
index 9938b37..b1d28a7 100644
--- a/node_modules/bitcore-lib-cash/lib/crypto/random.js
+++ b/node_modules/bitcore-lib-cash/lib/crypto/random.js
@@ -5,27 +5,39 @@ function Random() {
 
 /* secure random bytes that sometimes throws an error due to lack of entropy */
 Random.getRandomBuffer = function(size) {
-  if (process.browser)
+  // Detect browser environment
+  var isBrowser = typeof window !== 'undefined' && typeof window.document !== 'undefined';
+
+  if (isBrowser)
     return Random.getRandomBufferBrowser(size);
   else
     return Random.getRandomBufferNode(size);
 };
 
 Random.getRandomBufferNode = function(size) {
-  var crypto = require('crypto');
-  return crypto.randomBytes(size);
+  // Use @noble/hashes which works in both Node.js and browser
+  var randomBytes = require('@noble/hashes/utils.js').randomBytes;
+  return Buffer.from(randomBytes(size));
 };
 
 Random.getRandomBufferBrowser = function(size) {
-  if (!window.crypto && !window.msCrypto)
+  if (typeof window === 'undefined') {
+    throw new Error('Not in browser environment');
+  }
+
+  if (!window.crypto && !window.msCrypto) {
     throw new Error('window.crypto not available');
+  }
 
-  if (window.crypto && window.crypto.getRandomValues)
-    var crypto = window.crypto;
-  else if (window.msCrypto && window.msCrypto.getRandomValues) //internet explorer
-    var crypto = window.msCrypto;
-  else
+  var crypto;
+  if (window.crypto && window.crypto.getRandomValues) {
+    crypto = window.crypto;
+  } else if (window.msCrypto && window.msCrypto.getRandomValues) {
+    // Internet Explorer
+    crypto = window.msCrypto;
+  } else {
     throw new Error('window.crypto.getRandomValues not available');
+  }
 
   var bbuf = new Uint8Array(size);
   crypto.getRandomValues(bbuf);
@@ -40,7 +52,7 @@ Random.getPseudoRandomBuffer = function(size) {
   var b = Buffer.alloc(size);
   var r;
 
-  for (var i = 0; i <= size; i++) {
+  for (var i = 0; i < size; i++) {
     var j = Math.floor(i / 4);
     var k = i - j * 4;
     if (k === 0) {
diff --git a/node_modules/bitcore-lib-cash/lib/encoding/bufferwriter.js b/node_modules/bitcore-lib-cash/lib/encoding/bufferwriter.js
index ff38d1d..46870ed 100644
--- a/node_modules/bitcore-lib-cash/lib/encoding/bufferwriter.js
+++ b/node_modules/bitcore-lib-cash/lib/encoding/bufferwriter.js
@@ -1,7 +1,7 @@
 'use strict';
 
 var bufferUtil = require('../util/buffer');
-var assert = require('assert');
+function assert(val, msg) { if (!val) throw new Error(msg || 'Assertion failed'); }
 
 var BufferWriter = function BufferWriter(obj) {
   if (!(this instanceof BufferWriter))
diff --git a/node_modules/bitcore-lib-cash/lib/hdprivatekey.js b/node_modules/bitcore-lib-cash/lib/hdprivatekey.js
index a42edbc..9d6bd93 100644
--- a/node_modules/bitcore-lib-cash/lib/hdprivatekey.js
+++ b/node_modules/bitcore-lib-cash/lib/hdprivatekey.js
@@ -1,7 +1,7 @@
 'use strict';
 
 
-var assert = require('assert');
+function assert(val, msg) { if (!val) throw new Error(msg || 'Assertion failed'); }
 var buffer = require('buffer');
 var _ = require('lodash');
 var $ = require('./util/preconditions');
diff --git a/node_modules/bitcore-lib-cash/lib/hdpublickey.js b/node_modules/bitcore-lib-cash/lib/hdpublickey.js
index 202e95e..efcdd18 100644
--- a/node_modules/bitcore-lib-cash/lib/hdpublickey.js
+++ b/node_modules/bitcore-lib-cash/lib/hdpublickey.js
@@ -15,7 +15,7 @@ var PublicKey = require('./publickey');
 var bitcoreErrors = require('./errors');
 var errors = bitcoreErrors;
 var hdErrors = bitcoreErrors.HDPublicKey;
-var assert = require('assert');
+function assert(val, msg) { if (!val) throw new Error(msg || 'Assertion failed'); }
 
 var JSUtil = require('./util/js');
 var BufferUtil = require('./util/buffer');
diff --git a/node_modules/bitcore-lib-cash/lib/uri.js b/node_modules/bitcore-lib-cash/lib/uri.js
index 57ee36b..bf9a826 100644
--- a/node_modules/bitcore-lib-cash/lib/uri.js
+++ b/node_modules/bitcore-lib-cash/lib/uri.js
@@ -1,7 +1,6 @@
 'use strict';
 
 var _ = require('lodash');
-var URL = require('url');
 
 var Address = require('./address');
 var Networks = require('./networks');
@@ -109,16 +108,28 @@ URI.isValid = function(arg, knownParams) {
  * @returns {Object} An object with the parsed params
  */
 URI.parse = function(uri) {
-  var info = URL.parse(uri, true);
-  if (Networks.get( info.protocol.replace(':', '') ,'prefix')) {
-    // workaround to host insensitiveness
-    var group = /[^:]*:\/?\/?([^?]*)/.exec(uri);
-    info.query.address = group && group[1] || undefined;
-
-    return info.query;
-} else {
-  throw new TypeError('Invalid bitcoin URI');
+  // Browser-compatible URL parsing (no Node.js 'url' module required).
+  var protocolMatch = /^([^:]+):/.exec(uri);
+  if (!protocolMatch) throw new TypeError('Invalid bitcoin URI');
+  var protocol = protocolMatch[1];
+  if (!Networks.get(protocol, 'prefix')) throw new TypeError('Invalid bitcoin URI');
+
+  // Extract query parameters
+  var query = {};
+  var qIndex = uri.indexOf('?');
+  if (qIndex !== -1) {
+    var pairs = uri.slice(qIndex + 1).split('&');
+    for (var i = 0; i < pairs.length; i++) {
+      var kv = pairs[i].split('=');
+      query[decodeURIComponent(kv[0])] = decodeURIComponent(kv[1] || '');
+    }
   }
+
+  // Extract address (everything between "protocol:" and "?")
+  var group = /[^:]*:\/?\/?([^?]*)/.exec(uri);
+  query.address = group && group[1] || undefined;
+
+  return query;
 };
 
 URI.Members = ['address', 'amount', 'message', 'label', 'r'];
@@ -204,11 +215,13 @@ URI.prototype.toString = function() {
   }
   _.extend(query, this.extras);
 
-  return URL.format({
-    protocol: Networks.get(this.network ,'name').prefix + ':',
-    host: this.address.toString(true),
-    query: query
-  });
+  // Browser-compatible URL formatting (no Node.js 'url' module required).
+  var prefix = Networks.get(this.network, 'name').prefix;
+  var address = this.address.toString(true);
+  var qs = Object.keys(query)
+    .map(function(k) { return encodeURIComponent(k) + '=' + encodeURIComponent(query[k]); })
+    .join('&');
+  return prefix + ':' + address + (qs ? '?' + qs : '');
 };
 
 /**
diff --git a/node_modules/bitcore-lib-cash/lib/util/buffer.js b/node_modules/bitcore-lib-cash/lib/util/buffer.js
index 70f3817..8841a46 100644
--- a/node_modules/bitcore-lib-cash/lib/util/buffer.js
+++ b/node_modules/bitcore-lib-cash/lib/util/buffer.js
@@ -1,7 +1,7 @@
 'use strict';
 
 var buffer = require('buffer');
-var assert = require('assert');
+function assert(val, msg) { if (!val) throw new Error(msg || 'Assertion failed'); }
 
 var js = require('./js');
 var $ = require('./preconditions');
